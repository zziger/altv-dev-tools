{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild ./index.ts --bundle --watch --outfile=../dist/server.js --define:process.env.NODE_ENV=\\\"development\\\"",
    "build": "esbuild ./index.ts --bundle --outfile=../dist/server.js --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.13.10",
    "typescript": "^4.4.4"
  },
  "devDependencies": {
    "@altv/types-client": "^1.9.0",
    "@altv/types-server": "^2.0.7"
  }
}
